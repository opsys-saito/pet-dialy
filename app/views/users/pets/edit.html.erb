<div>
  <h2><%= Pet.model_name.human %>編集</h2>
  <%= bootstrap_form_with model: @pet, url: users_pet_path(@pet), label_errors: true, html: { novalidate: true } do |f| %>
    <%= render "form", f: %>
    <%= f.submit "更新" %>
  <% end %>
</div>
